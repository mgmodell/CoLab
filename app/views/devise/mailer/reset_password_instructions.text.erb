Hello <%= @resource.informal_name( false ) %>!

Someone has requested a change to your CoLab password. Follow these steps to do so:

1. Navigate to <%= root_url %>.
2. Log out if you are currently logged in.
3. Click this password reset link. <%= edit_password_url(@resource, reset_password_token: @token) %>.
4. Enter and confirm your new password in the page.
5. Click the 'Change my password' button.

...and you're done!

If you didn't request this, please ignore this email.
Your password won't change until you access the link above and create a new one.

CoLab Support
