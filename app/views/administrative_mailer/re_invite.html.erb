<!DOCTYPE html>
<html>
    <head>
          <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
    </head>
    <body>
          <h1>Hello!</h1>
          <p>
          You've been invited to participate in the following
          course<%= "s" unless @user.rosters.count < 2 %>:
          </P>
          <ul>
            <% @user.rosters.each do |roster| %>
            <li><%= roster.course.name %></li>
            <% end %>
          </ul>
          <P>
          Please log into <%= link_to root_url, root_url %> to
          accept or decline this enrollment.
          <P>
          Thank you!
          </P>
          <P>
          CoLab Support
          </p>
     </body>
</html>
