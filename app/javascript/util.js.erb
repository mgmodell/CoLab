<% include Rails.application.routes.url_helpers %>

export default function getURL( service ){
  var url = '';

  switch( service ){
    case 'quote':
      url = '<%= get_quote_path %>';
  }

  return url;
}
