en:
  required:
    errors:
      models:
        diagnosis:
          attributes:
            behavior:
              required: You must select a behavior.
        group:
          attributes:
            project:
              required: A group must be attached to a project.
        project:
          attributes:
            style:
              required: Assessment style must be set.
